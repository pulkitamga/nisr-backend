<div
    id="branch-stock-history-config"
    data-history-url="{{ route('admin.branch.stock-history') }}"
    data-loading="{{ translate('Loading...') }}"
    data-failed="{{ translate('Unable to load stock history') }}"
    data-no-history="{{ translate('No transfer history found') }}"
    data-default-variation="{{ translate('Default') }}"
></div>
