<div
    id="branch-stock-approval-config"
    data-approve-title="{{ translate('Approve') }}"
    data-approve-text="{{ translate('Are you sure you want to approve this stock transfer?') }}"
    data-approve-confirm="{{ translate('Yes, approve it') }}"
    data-reject-title="{{ translate('Reject') }}"
    data-reject-text="{{ translate('Are you sure you want to reject this stock transfer?') }}"
    data-reject-confirm="{{ translate('Yes, reject it') }}"
    data-cancel="{{ translate('Cancel') }}"
></div>
